# Локальная разработка: Vite проксирует /api и /events на http://localhost:3000,
# поэтому переменные можно не задавать. Оставлены для docker-сборки.

# REST API base URL. В docker nginx проксирует /api -> backend, поэтому тут /api.
VITE_API_URL=/api
# WebSocket URL событий.
VITE_WS_URL=/events
